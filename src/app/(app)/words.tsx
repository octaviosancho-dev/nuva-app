import { Unbuilt } from '@/components/app/Unbuilt';

export default function WordsScreen() {
  return (
    <Unbuilt
      title="Find your words"
      summary="Two or three sentences you can say at your appointment, built from what you have logged. Not a chart — the words."
      milestone="Milestone 8"
    />
  );
}
